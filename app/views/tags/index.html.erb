<br>
<h1><strong>Lista de etiquetas actuales en el sitio</strong></h1>
<h3><strong><%= link_to 'Agregar nueva etiqueta', new_tag_path %></strong></h3>
<h5><%= link_to 'Volver atras', users_show_path(:id => current_user.id) %></h5>

<table class="table table-striped">
  <thead>
    <tr>
      <th>Nombre</th>
      <th>Modificar</th>
      <th>Eliminar</th>
      
    </tr>
  </thead>

  <tbody>
    <% @tags.each do |tag| %>
      <tr>
        <% if tag.marca == false %>
          <td><%= tag.nombre %></td>
          <td><%= link_to 'Modificar', edit_tag_path(tag) %></td>
          <td><%= link_to 'eliminar', tags_blogico_path(:id => tag.id,:marca => true) %></td>
      </tr>
      <% end %>
    <% end %>
  </tbody>
</table>

<br>



