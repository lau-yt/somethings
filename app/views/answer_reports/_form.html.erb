<%= form_for([@answer, @answer_report]) do |form| %>
  <% if @answer_report.errors.any? %>
    <div id="error_explanation">
      <h2> Ooops hubo un error en el reporte!</h2>

      <ul>
      <% answer_report.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <h4><strong>Cuentanos el motivo de tu denuncia!</strong></h4>
  <div class="field">
    <%= form.text_area :titulo, id: :answer_report_titulo %>
  </div>

  <div class="actions">
    <%= form.submit "Enviar reporte"%>
  </div>
<% end %>
