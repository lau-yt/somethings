<h1>Denuncias en comentarios de una respuesta</h1>

<% if @comment_report_answers.size == 0 %>
  <h4><strong>Vaya! Todavia no hay denuncias aqui!</strong></h4>
<% else %>
<table class="table table-bordered" style="background: rgba(82, 221, 41, 0.35)">
  <thead>
    <tr  style="background: rgba(131, 159, 193, 0.35)">
      <th>Descripcion</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @comment_report_answers.each do |c| %>
      <tr>
        <td><%=c.titulo %></td>
        <td><%= link_to 'Ver comentario denunciado',c.comment_answer %></td>
        <td><%= link_to 'Eliminar denuncia',c, method: :delete, data: { confirm: 'Estas seguro de eliminar la denuncia?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
<% end %>
<%= link_to 'Volver al menu de reportes', reports_index_path %>