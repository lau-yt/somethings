<%= form_for([@question, @question_report]) do |form| %>
  <% if @question_report.errors.any? %>
    <div id="error_explanation">
      <h2> Ooops hubo un error en el reporte!</h2>

      <ul>
      <% question_report.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <h4><strong>Cuentanos el motivo de tu denuncia!</strong></h4>
  <div class="field">
    <%= form.text_area :titulo, id: :question_report_titulo %>
  </div>

  <div class="actions">
    <%= form.submit "Enviar reporte"%>
  </div>
<% end %>
