<%= form_with(model: comment_report_question, local: true) do |form| %>
  <% if comment_report_question.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(comment_report_question.errors.count, "error") %> prohibited this comment_report_question from being saved:</h2>

      <ul>
      <% comment_report_question.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :titulo %>
    <%= form.text_field :titulo, id: :comment_report_question_titulo %>
  </div>

  <div class="field">
    <%= form.label :comment_question_id %>
    <%= form.number_field :comment_question_id, id: :comment_report_question_comment_question_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
